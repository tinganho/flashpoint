directive @directive(argument: [ID]!) on OBJECT

type Object @directive(argument: ["id", "id"]) {
field: Int
}

enum Enum {
    ENUM_VALUE
}

END_OF_DOCUMENT:
!!! No query root operation type defined.